const express = require('express'); const { Connection, PublicKey } = require('@solana/web3.js'); require('dotenv').config(); const app = express(); const connection = new Connection(process.env.RPC_ENDPOINT); app.get('/stake-account/:address', async (req, res) => { try { const stakeAccount = await connection.getAccountInfo(new PublicKey(req.params.address)); res.json({ address: req.params.address, balance: stakeAccount?.lamports || 0, owner: stakeAccount?.owner.toString() || 'unknown' }); } catch (error) { res.status(400).json({ error: error.message }); } }); app.listen(process.env.PORT || 3000, () => { console.log(`API running on port ${process.env.PORT || 3000}`); });